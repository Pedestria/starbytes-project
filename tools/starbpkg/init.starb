import IO

decl requestProjectPath = "tools/starbpkg/.request/project.txt"
secure(decl project = IO.readText(requestProjectPath,"utf-8")) catch {}

decl modpathsPath = project + "/.starbpkg/modpaths.txt"
decl outputPath = project + "/.starbmodpath"

secure(decl modpathsText = IO.readText(modpathsPath,"utf-8")) catch {}
secure(decl outputWrite = IO.writeText(outputPath,modpathsText,"utf-8")) catch {}

print("starbpkg: initialized " + project)
